# 🧭 Typography Guidelines

> Гайдлайн по типографике для проекта на основе shadcn/ui  
> Используется в связке с универсальным компонентом `<Typography variant="..." as="...">`.

---

## 📘 Общие принципы

- **Контраст и иерархия:** крупнее → важнее.  
- **Меньше шрифтов — больше ритма:** один шрифт, разные веса и размеры.  
- **Стилизация через Tailwind токены:**  
  Используй `text-muted-foreground`, `tracking-tight`, `font-semibold`, `leading-7` и другие утилиты, совместимые с темой shadcn/ui.

---

## 🧩 Варианты типографики (`variant`)

| Variant | Назначение | Тег по умолчанию | Tailwind-стиль | Где применять |
|----------|-------------|------------------|----------------|----------------|
| **h1** | Главный заголовок страницы | `<h1>` | `text-4xl lg:text-5xl font-extrabold tracking-tight` | В верхней части страницы (например, Dashboard, Настройки) |
| **h2** | Подзаголовок секции | `<h2>` | `text-3xl font-semibold tracking-tight` | Заголовки крупных секций |
| **h3** | Подраздел / блок | `<h3>` | `text-2xl font-semibold` | Внутри карточек, блоков, списков |
| **h4** | Мелкий заголовок | `<h4>` | `text-xl font-semibold` | Заголовки таблиц, панелей |
| **p** | Основной текст | `<p>` | `leading-7 [&:not(:first-child)]:mt-6` | Абзацы, описания |
| **lead** | Вводный подзаголовок | `<p>` | `text-xl text-muted-foreground` | Под основным заголовком страницы |
| **large** | Крупный акцентный текст | `<p>` | `text-lg font-semibold` | Метрики, суммы, ключевые данные |
| **small** | Мелкий вспомогательный текст | `<small>` | `text-sm font-medium leading-none` | Подписи, заголовки колонок |
| **muted** | Второстепенный текст | `<p>` | `text-sm text-muted-foreground` | Сноски, пояснения, подписи |
| **code** | Встроенный моноширинный код | `<code>` | `rounded bg-muted px-[0.3rem] py-[0.2rem] font-mono text-sm` | Для кода, команд, ключей API |

---

## 🧭 Иерархия применения

| Уровень | Пример применения | Рекомендуемый variant |
|----------|-------------------|------------------------|
| 1️⃣ | Заголовок страницы | `h1` |
| 2️⃣ | Раздел страницы | `h2` |
| 3️⃣ | Подраздел / карточка | `h3` |
| 4️⃣ | Мелкий заголовок | `h4` |
| 📄 | Текст, описание | `p`, `lead` |
| ⚙️ | Подписи, сноски | `muted`, `small` |
| 💻 | Код, команды | `code` |

---

## 🧠 UX-рекомендации

- Не начинай страницу с `h2`, `h3` и т.д. — соблюдай семантику.  
- `lead` всегда следует **сразу после `h1`**.  
- Для карточек (`Card`) чаще всего подходят `h3`, `h4` и `muted`.  
- `large` используй для визуальных акцентов (например, цифр, статусов).  
- Второстепенные тексты оформляй через `muted`, а не просто `opacity`.  
- `code` предназначен только для **инлайн-фрагментов**; для блоков кода — отдельный компонент (`CodeBlock`).  
- Старайся поддерживать вертикальный ритм (отступы между абзацами и заголовками должны быть согласованы).

---

## 🎨 Примеры использования

```tsx
<section className="space-y-4">
  <Typography variant="h1">Настройки аккаунта</Typography>
  <Typography variant="lead">
    Управляйте безопасностью и данными своего профиля.
  </Typography>

  <Typography variant="h2">Пароль и доступ</Typography>
  <Typography variant="p">
    Здесь вы можете изменить свой пароль или включить двухфакторную аутентификацию.
  </Typography>

  <Typography variant="h3">API ключ</Typography>
  <Typography variant="code">sk_live_3fsdf9asf9a...</Typography>
  <Typography variant="muted">Не передавайте этот ключ третьим лицам.</Typography>
</section>
